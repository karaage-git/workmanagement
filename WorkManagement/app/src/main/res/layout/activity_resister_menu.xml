<?xml version="1.0" encoding="utf-8"?>
<androidx.constraintlayout.widget.ConstraintLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    tools:context=".view.ResisterMenuActivity">

    <androidx.appcompat.widget.Toolbar
        android:id="@+id/toolbar"
        style="@style/MyToolBar"
        tools:ignore="MissingConstraints" />

    <Button
        android:id="@+id/btn_normal"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:text="@string/btn_resistermenu_normal"
        android:layout_marginStart="@dimen/margin_top_menu_side"
        android:layout_marginEnd="@dimen/margin_top_menu_side"
        app:layout_constraintBottom_toBottomOf="@id/btn_bonus"
        app:layout_constraintLeft_toLeftOf="parent"
        app:layout_constraintRight_toRightOf="parent"
        app:layout_constraintTop_toBottomOf="@id/toolbar" />


    <Button
        android:id="@+id/btn_bonus"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:text="@string/btn_resistermenu_bonus"
        android:layout_marginStart="@dimen/margin_top_menu_side"
        android:layout_marginEnd="@dimen/margin_top_menu_side"
        app:layout_constraintBottom_toBottomOf="parent"
        app:layout_constraintLeft_toLeftOf="parent"
        app:layout_constraintRight_toRightOf="parent"
        app:layout_constraintTop_toTopOf="@+id/btn_normal" />

</androidx.constraintlayout.widget.ConstraintLayout>